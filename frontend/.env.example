# Backend API URL — set this in Netlify dashboard under Site Settings > Environment Variables
# For local dev, leave empty (Vite proxy handles it)
# VITE_API_URL=https://your-backend.onrender.com/api
